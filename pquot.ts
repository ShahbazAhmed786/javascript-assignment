function QuoteAndAuthor(quote: string, author: string): void {
  console.log(`${author} once said, "${quote}"`);
}

const quote: string = "The ultimate aim of the ego is not to see something, but to be something.";
const author: string = "Allma Muhammad Iqbal";
QuoteAndAuthor(quote, author);