function Message(message: string): void {
  console.log(message);
}

const famousPerson: string = "Allma Muhammad Iqbal";
const quote: string = "The ultimate aim of the ego is not to see something, but to be something.";

const message: string = `${famousPerson} once said, "${quote}"`;
Message(message);




